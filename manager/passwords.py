from manager.storage import load, save

passwords = load()

passwords = []
def add_password(save(passwords)):
    passwords.append({
        "site": site,
        "login": login,
        "password": password
    })
def list_passwords():
    return passwords
